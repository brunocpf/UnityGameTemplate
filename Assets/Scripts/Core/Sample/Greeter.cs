namespace __GAME_NAMESPACE__.Core.Sample
{
    /// <summary>
    /// Placeholder demonstrating a pure-domain Core class with no Unity dependencies.
    /// Replace this with your own domain types when scaffolding a new feature.
    /// </summary>
    public sealed class Greeter
    {
        public string Greet(string name) => $"Hello, {name}!";
    }
}
